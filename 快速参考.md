# 🔧 FN标注工具 - 快速参考

## 📦 你现在拥有的工具

### 1. **auto_annotate_fn.py** ⚡
   快速自动分类漏检样本的命令行工具

   **最常用的3条命令：**
   ```bash
   # 一键自动标注（最简单）
   python auto_annotate_fn.py --input data.csv
   
   # 保存到新文件（不覆盖原文件）
   python auto_annotate_fn.py --input data.csv --output output.csv
   
   # 只标注未标注的行（继续之前的工作）
   python auto_annotate_fn.py --input data.csv --skip-annotated
   ```

   **输出**：
   - ✅ 自动原因分类
   - ✅ 置信度 (0-1)
   - ✅ 是否标注问题
   - ✅ 待复核 (True/False)

---

### 2. **annotate_fn_enhanced.py** 🎨
   Web界面，用来快速审阅和修改自动标注结果

   **启动方法：**
   ```bash
   streamlit run annotate_fn_enhanced.py
   ```

   **主要功能：**
   - 📊 显示自动标注结果 + 置信度
   - 🖼️ 左右对比查看漏检图和Query原图
   - ⌨️ 快捷键快速操作（1-6数字选择，0采用AI建议）
   - 🔍 3种过滤模式：全部 / 未标注 / 待复核
   - 💾 自动保存到CSV

   **快捷键速查：**
   | 键 | 作用 |
   |----|------|
   | 1-6 | 快速选择原因（遮挡/尺寸/光照/背景/标注/其他） |
   | 0 | 采用AI建议 |
   | 回车(空) | 采用AI或当前选择 |

---

### 3. **annotation_toolkit.py** 🛠️
   Python编程接口 + 使用指南

   **编程方式使用：**
   ```python
   from annotation_toolkit import AnnotationToolkit
   
   # 自动标注
   AnnotationToolkit.auto_annotate("data.csv")
   
   # 启动Web
   AnnotationToolkit.launch_web_ui("data.csv")
   
   # 批量处理
   AnnotationToolkit.batch_process(
       ["data1.csv", "data2.csv"],
       output_dir="outputs"
   )
   ```

---

## 🚀 推荐工作流

### 场景1：快速验证小数据（<1000样本）

```
Step 1: 自动标注 (10秒)
   python auto_annotate_fn.py --input data.csv

Step 2: 人工审阅 (10分钟)
   streamlit run annotate_fn_enhanced.py
   → 过滤"待复核"
   → 用快捷键0快速确认
```

### 场景2：处理大数据（4000+样本）

```
Step 1: 自动标注 (1分钟)
   python auto_annotate_fn.py --input data.csv

Step 2: 有针对性的复核 (30分钟)
   streamlit run annotate_fn_enhanced.py
   → 过滤"待复核"（通常只有5-20%）
   → 快速改正错误分类

Step 3: 补充手动标注（可选）
   → 过滤"未标注"
   → 对AI无法分类的进行标注
```

### 场景3：批量处理多个文件

```python
# batch_process.py
from annotation_toolkit import AnnotationToolkit

files = [
    "data/batch1.csv",
    "data/batch2.csv",
    "data/batch3.csv"
]

AnnotationToolkit.batch_process(files, output_dir="output")
```

---

## 🎯 关键特性

| 特性 | auto_annotate_fn | annotate_fn_enhanced |
|------|-----------------|----------------------|
| 自动分类 | ✅ | - |
| 置信度评分 | ✅ | ✅ 显示 |
| Web界面 | - | ✅ |
| 图片预览 | - | ✅ |
| 快捷键 | - | ✅ |
| 过滤筛选 | - | ✅ |
| 命令行 | ✅ | ✅ |
| Python API | ✅ | ✅ |

---

## 💦 输入数据格式

支持的CSV列（自动识别）：
- `image_path` - 漏检样本图像路径
- `query_image_path` - Query原图路径
- `mode` - "text" 或 "image"
- `query_text` - 文本查询内容（文本检索时）
- 其他任意列用于特征匹配

**必需的列：**无（脚本自动适应）

---

## 📊 输出结果示例

运行自动标注后，CSV新增列：

```
| ID | 原始数据 | 自动原因分类 | 置信度 | 是否标注问题 | 待复核 | 人工原因分类 |
|----|--------|-----------|--------|------------|--------|------------|
| 1  | {...}  | 光照/模糊 | 0.85   | 否        | False  | (为空，等待人工标注) |
| 2  | {...}  | 遮挡     | 0.42   | 是        | True   | (为空) |
| 3  | {...}  | 目标尺寸 | 0.91   | 否        | False  | (为空) |
```

---

## ❓ 常见操作

**Q: 想跳过某些已标注的行**
```bash
python auto_annotate_fn.py --input data.csv --skip-annotated
```

**Q: 置信度太低想调整敏感度**
```bash
# 提高阈值，更多的结果会被标记为待复核
python auto_annotate_fn.py --input data.csv --confidence-threshold 0.5
```

**Q: 想只标注"待复核"的结果**
```
1. 启动 streamlit run annotate_fn_enhanced.py
2. 顶部选择"待复核"过滤模式
3. 逐个审阅并修改
```

**Q: Python中想快速调用**
```python
from annotation_toolkit import AnnotationToolkit
AnnotationToolkit.auto_annotate("my_data.csv")
```

---

## 📈 性能参考

| 数据量 | 自动标注时间 | 复核时间 |
|-------|-----------|--------|
| 100 | <1秒 | 5分钟 |
| 1000 | 1-2秒 | 30分钟 |
| 4000 | 3-5秒 | 2小时（仅复核待复核部分） |
| 10000+ | 10秒 | 按需分批 |

---

## 🔍 调试技巧

**自动标注没有识别出原因？**
- 检查样本特征文本中是否包含关键词
- 可能需要扩展 `FN_REASON_KEYWORDS` 字典

**置信度分数都很低？**
- CSV中缺少特征列
- 原始数据格式与预期不符
- 可能需要自定义特征提取逻辑

**Web界面打不开？**
```bash
# 检查Streamlit是否安装
pip install streamlit

# 或指定端口
streamlit run annotate_fn_enhanced.py --server.port 8502
```

---

## 📝 文案总结

现在你有了一个**完整的自动化标注系统**：

1. ⚡ **自动标注** - 一条命令完成4000+样本的快速分类
2. 🎨 **Web审阅** - 友好的界面快速复核和修改
3. 🔧 **编程接口** - 灵活的Python API支持集成

**推荐首先这样做：**
```bash
# Step 1: 快速自动标注
python auto_annotate_fn.py --input your_data.csv

# Step 2: 启动Web审阅
streamlit run annotate_fn_enhanced.py

# Step 3: 在Web界面中
# - 过滤"待复核"，快速用快捷键0确认AI结果
# - 对确实错误的进行人工修改
# - 完成！
```

就这么简单！🎉
